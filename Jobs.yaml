apiVersion: batch/v1
kind: Job
metadata:
  name: mybackup-job
spec:
  template:
    spec:
      containers:
      - name: mybackup-container
        image: busybox
        command:
        - /bin/sh
        - -c
        - "echo Backup Started && sleep 30 && echo Backup Completed"
      restartPolicy: Never