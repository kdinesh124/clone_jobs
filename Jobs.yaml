apiVersion: batch/v1
kind: Job
metadata:
  name: mybackup-job
spec:
  backoffLimit: 4
  template:
    metadata:
      name: mybackup-pod
 spec:
  template:
    spec:
      containers:
      - name: mybackup-container
        image: busybox
      restartPolicy: Never